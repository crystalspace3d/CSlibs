Index: bootstrap
===================================================================
--- bootstrap	(Revision 2337)
+++ bootstrap	(Arbeitskopie)
@@ -3,4 +3,4 @@
 export WANT_AUTOMAKE='1.9'
 export WANT_AUTOCONF_2_5=1
 
-libtoolize --force && aclocal $ACLOCAL_FLAGS && autoheader && automake --include-deps --add-missing --foreign && autoconf 
+libtoolize --force -c && aclocal $ACLOCAL_FLAGS && autoheader && automake --include-deps --add-missing --foreign -c && autoconf 
