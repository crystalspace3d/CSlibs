Index: src/CEGUIDynamicModule.cpp
===================================================================
--- src/CEGUIDynamicModule.cpp	(revision 1251)
+++ src/CEGUIDynamicModule.cpp	(working copy)
@@ -30,7 +30,7 @@
 #include "CEGUIString.h"
 #include "CEGUIExceptions.h"
 
-#if defined(__WIN32__) || defined(_WIN32)
+#if defined(__WIN32__) || defined(_WIN32) || defined(__CYGWIN__)
 #   if defined(_MSC_VER)
 #       pragma warning(disable : 4552)  // warning: operator has no effect; expected operator with side-effect
 #   endif
@@ -63,6 +63,18 @@
         d_moduleName.insert(0, "lib");
 #endif
 
+    // cs-win32libs: Prefix & suffix
+#ifdef CEGUI_FACTORYMODULE_PREFIX
+    d_moduleName.insert(0, CEGUI_FACTORYMODULE_PREFIX);
+#endif
+#ifdef CEGUI_FACTORYMODULE_SUFFIX
+    if (d_moduleName.substr(d_moduleName.length() - 4, 4) == ".dll")
+      d_moduleName.insert(d_moduleName.length() - 4, 
+	CEGUI_FACTORYMODULE_SUFFIX);
+    else
+      d_moduleName += CEGUI_FACTORYMODULE_SUFFIX;
+#endif
+	  
     // Optionally add a _d to the module name for the debug config on Win32
 #if defined(__WIN32__) || defined(_WIN32)
 #   if defined (_DEBUG) && defined (CEGUI_LOAD_MODULE_APPEND_SUFFIX_FOR_DEBUG)
